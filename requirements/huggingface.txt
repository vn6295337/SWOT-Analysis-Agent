# Requirements for Hugging Face Spaces deployment
# Optimized for HF Spaces environment constraints

fastapi==0.111.0
uvicorn==0.30.1
python-dotenv==1.2.1
pydantic==2.7.1

# Note: Additional dependencies may be installed conditionally
# based on runtime environment detection
