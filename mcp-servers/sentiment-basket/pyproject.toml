[project]
name = "sentiment-basket"
version = "1.0.0"
description = "MCP server for sentiment analysis from Finnhub, Reddit, and YouTube"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
